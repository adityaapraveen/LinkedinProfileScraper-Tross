import { unsupportedManifest, type EndpointManifest } from './endpoint-manifest.js';

export const educationEndpoint: EndpointManifest = unsupportedManifest(
  'education.v1',
  'A sanitized captured education request and response are required',
);
